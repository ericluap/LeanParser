# LeanParser
Impelements Lean's parsing algorithm.

- Run the arithmetic example (`examples/Arithmetic.hs`) with `stack run arithmetic`
- Run the language example (`examples/Language.hs`) with `stack run language`
- Run the tests with `stack test`